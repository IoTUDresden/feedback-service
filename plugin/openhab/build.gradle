jar {
    baseName = 'feedback-plugin-openhab'
    version =  '1.0.0-SNAPSHOT'
}

dependencies {
    compile project(':service-api')

    compile 'org.springframework.boot:spring-boot-starter-websocket'
}