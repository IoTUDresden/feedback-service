jar {
    baseName = 'feedback-plugin-openhab'
    version =  '1.0.0-SNAPSHOT'
}

dependencies {
    compile project(':service-api')

    compile 'com.netflix.feign:feign-core:8.10.0',
            'com.netflix.feign:feign-jackson:8.10.0'
}